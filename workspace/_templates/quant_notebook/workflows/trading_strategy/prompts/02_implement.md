# Strategy Implementation

Implement the strategy from the research above in Python.

Requirements:
- pandas/numpy for data manipulation
- No look-ahead bias (use .shift(1) for signal-to-trade lag)
- Include transaction costs (default: 0.1% per trade)
- Separate Signal class and Backtest class
- Type hints on all functions
- One example backtest run with synthetic or sample data

Output complete Python files.
