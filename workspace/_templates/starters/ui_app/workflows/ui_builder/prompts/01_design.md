# UI Component Design

Design the UI component or feature described by the user.

Output:
1. **Component name and purpose**
2. **Props/inputs**: what data it receives
3. **State**: what local state it manages
4. **Events/callbacks**: what it emits to parent
5. **Visual layout** (ASCII diagram if helpful)
6. **Accessibility considerations**
7. **Files to create/modify**
