name: "{{PROJECT_NAME}}"
description: "Project created from blank template"
code_dir: "../../{{PROJECT_NAME}}"
default_provider: claude
active_workflows: []
commit_code_extraction:
  min_lines: 5
  only_on_commits_with_tags: false
