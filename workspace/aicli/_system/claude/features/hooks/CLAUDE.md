# Feature: hooks
_Feature: hooks | 2026-04-15 01:04 UTC_

## Requirements

Hooks system for aicli that enables triggering workflows, agent actions, and memory operations in response to project events (prompt batches, commits, CLI commands, session starts/ends, memory generation). Hooks should support multiple trigger types (event_type-based filtering), configurable actions (embed, promote_work_item, regenerate_memory, run_pipeline), and flexible persistence (database-backed for durability, optional webhook for external integrations). Hook templates for common workflows (log_session_stop, session_start_analysis, daily_memory_refresh) provided as scaffolding. Hooks integrated into existing backend routers (route_memory, route_history, route_work_items) and callable from CLI via `/hook` command or auto-trigger on lifecycle events.

## Action Items

1. Define hook schema (mng_hooks table: id, client_id, project_id, name, event_type, condition_filter, actions JSONB, enabled, created_at, updated_at) and migration (m047_create_mng_hooks.sql)
2. Implement HookManager class (core/hooks.py) with fire(event_type, payload), list(), create(), update(), delete() methods
3. Add hook trigger points to existing routers: route_history (on commit sync), route_memory (on embed-prompts completion), route_work_items (on patch work_item), route_sessions (on session_start/stop)
4. Create `/hook/{project}` endpoints: GET (list), POST (create), PATCH /{hook_id} (update), DELETE /{hook_id} (delete), POST /test (dry-run)
5. Add hook template files to workspace/aicli/templates/hooks/ (log_session_stop.yaml, session_start_analysis.yaml, daily_memory_refresh.yaml)
6. Integrate hook CLI commands into CLI handler (aicli hook list/create/delete/test)
7. Implement background job scheduler for time-based hooks (APScheduler integration)
8. Add webhook support (HTTP POST to external endpoints) with retry/timeout handling
9. Document hook system in MEMORY.md with examples and troubleshooting
10. Test end-to-end: trigger hook via commit sync, verify actions execute in order, confirm side effects (new events created, memory regenerated, pipeline executed)

---
_Auto-generated by aicli. Run `/memory` to refresh._