# Feature: memory
_Feature: memory | 2026-04-15 01:03 UTC_

## Requirements

Memory system provides 4-layer architecture for capturing, synthesizing, and retrieving project context: (1) ephemeral session state → (2) raw mirror capture (mem_mrr_*) via prompt/commit/tag ingestion → (3) LLM digests in mem_ai_events with embeddings and semantic tagging → (4) work items/project facts distilled for long-term storage. System must tag events with user-facing metadata (phase, feature, bug, source), support backfill from historical commits without active sessions, link work items bidirectionally to events/commits/prompts, and regenerate memory files (/memory endpoint) from consolidated data. Must handle incremental processing, deduplication by hash, and multi-client/multi-project isolation via proper FK relationships (client_id → project_id).

## Action Items

1. Complete mem_ai_events restructuring: finalize column order (client_id → project_id → created_at/processed_at → content/event_type/llm_source → tags/embedding), migrate data, drop _old table
2. Implement tag consolidation: retain only user-facing tags (phase/feature/bug/source) in events; remove system metadata (llm, chunk_type, commit_hash)
3. Fix database schema errors: remove non-existent column references (lifecycle, event_type, c.id), define missing log variable in route handlers
4. Implement missing memory update logic: populate memory_items and project_facts tables on /memory endpoint run with proper trigger conditions
5. Add mng_projects table with project metadata, migrate all project references from text to project_id FK
6. Refactor planner_tags table: consolidate redundant columns (seq_num, source, multiple descriptors), keep creator/updater only
7. Implement work_item-to-work_item linking: add parent_id/merged_into FK with proper cascade, test drag-and-drop persistence
8. Wire AI tag approval flow: implement approve/remove buttons in Planner UI to confirm AI-suggested tags
9. Document complete memory pipeline: clarify trigger points, LLM prompts per stage, column sourcing, event filtering logic
10. Test full memory cycle: /memory endpoint should regenerate CLAUDE.md/MEMORY.md/context.md with accurate counts and proper tag filtering

---
_Auto-generated by aicli. Run `/memory` to refresh._