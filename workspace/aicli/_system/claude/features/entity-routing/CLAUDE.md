# Feature: entity-routing
_Feature: entity-routing | 2026-04-15 01:06 UTC_

## Requirements

entity-routing feature enables dynamic API endpoint resolution based on entity type (user, project, client, workspace). Route handlers detect entity kind from request context and delegate to specialized processors. System must support routing entity-specific operations (read, create, update, delete, list) with proper role-based access controls while maintaining separation between engine code and workspace content. Routing logic integrates with existing MCP tools, CLI commands, and work item pipeline stages.

## Action Items

1. Implement entity type detection middleware in route handlers; 2. Create entity processor registry mapping types to handler functions; 3. Add role-based authorization checks at route entry points; 4. Wire entity-routing into memory endpoints (/memory/{project}/entity-routing); 5. Integrate with work_item_pipeline stages for cross-entity references; 6. Update MCP create_entity tool to use new routing layer; 7. Document entity routing contract and add tests for multi-type scenarios.

---
_Auto-generated by aicli. Run `/memory` to refresh._