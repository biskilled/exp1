[PROJECT CONTEXT: aicli]
aicli is a shared AI memory platform that unifies history and context across multiple AI tools (Claude CLI, Cursor, ChatGPT, etc.) for developers. It stores unified prompt/response history per project in JSONL format, enables semantic search via PostgreSQL + pgvector, and provides a desktop UI (Electron + Vanilla JS) and CLI for managing sessions, tags, commits, and workflows. Current state (v2.2.0): commit-to-prompt linking complete, tag caching optimized, memory synthesis working, phase-based session management live, and multi-agent DAG workflows visualized via Cytoscape.
Stack: cli=Python 3.12 + prompt_toolkit + rich, backend=FastAPI + uvicorn + python-jose + bcrypt, frontend=Vanilla JS (no framework, no bundler) + Electron shell + Vite dev server, ui_components=xterm.js (embedded terminal) + Monaco editor + Cytoscape.js (graph flows), storage_primary=JSONL (history.jsonl, commit_log.jsonl), JSON, CSV — flat file first
In progress: Commit-to-prompt-to-session linking — source_id timestamp from history.jsonl maps to events in event_tags_{p}; POST /entities/events/tag-by-source-id creates bidirectional links between commits and prompts; multiple commits per session each tagged to their originating prompt (2026-03-14), Tag cache persistence in history tab — all categories/values loaded once on tab open via Promise.all; color preservation on save prevents DB thrashing; zero DB calls during tag picker operations (2026-03-14), CLAUDE.md memory layer alignment — verified synthesis logic matches multi-layer design; all recent features (nested tags, commit linking, session persistence, tag cache, graph workflows) captured in memory output; PROJECT.md v2.2.0 updated (2026-03-14)
Decisions: Engine/workspace separation: aicli/ = code only, workspace/ = per-project content, _system/ = project state; Flat file storage (JSONL/JSON) primary; PostgreSQL + pgvector for semantic search; per-project DB tables via project_table(); Electron UI with xterm.js + Monaco; Vanilla JS frontend — no React/Vue/build step; Vite dev server only
Last work (2026-03-14): I do see that there is a link between commit and session ID. is it possible to have link between commit/push and prompt ID ? commit are created after 