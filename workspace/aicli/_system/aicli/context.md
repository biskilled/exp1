[Project Facts] auth_pattern=login_as_first_level_hierarchy; data_persistence_issue=tags_disappear_on_session_switch; db_engine=SQL; deployment_target=Claude_CLI_and_LLM_platforms; mcp_integration=embedding_and_data_retrieval; memory_management_pattern=load_once_on_access_update_on_save

[PROJECT CONTEXT: aicli]
aicli is a shared AI memory platform combining a Python CLI, FastAPI backend, and Electron desktop UI to provide persistent semantic memory, project tracking, and multi-agent workflow orchestration across LLM providers (Claude, OpenAI, DeepSeek, Gemini, Grok). Currently at version 2.2.0, it uses PostgreSQL+pgvector for semantic search, JSONL for history with rotation, and an MCP server for tool integration; active focus is resolving PROJECT.md load performance issues and a project visibility regression on the free Railway tier.
Stack: cli=Python 3.12 + prompt_toolkit + rich, backend=FastAPI + uvicorn + python-jose + bcrypt, frontend=Vanilla JS (no framework, no bundler) + Electron shell + Vite dev server, ui_components=xterm.js (embedded terminal) + Monaco editor + Cytoscape.js (graph flows) + cytoscape-dagre, storage_primary=JSONL (history.jsonl with rotation to history_YYMMDDHHSS.jsonl, commit_log.jsonl), JSON, CSV
In progress: PROJECT.md load performance optimization (2026-03-17) — >1 minute load time on free Railway tier when opening aiCli project; investigating DB query latency vs file I/O bottleneck; considering pagination/lazy-loading, Project visibility issue (2026-03-17) — aiCli project disappeared from recent projects list; requires verification of openProject() function and project listing query logic, Multi-agent workflow system (2026-03-16) — Async DAG executor integration with Cytoscape.js visualization + YAML config for multi-agent prompt orchestration
Decisions: Engine/workspace separation: aicli/ = code only, workspace/ = per-project content, _system/ = project state; Flat file primary (JSONL with rotation on /memory); PostgreSQL + pgvector for semantic search; per-project DB tables with indexed columns (phase/feature/session_id); Electron UI with xterm.js + Monaco; Vanilla JS frontend (no React/Vue/bundler); Vite dev server for local dev
Entities: [bug] hooks (2ev) | [doc_type] high-level-design (1ev), low-level-design (1ev), Test (0ev) | [feature] shared-memory (3ev), test-picker-feature (0ev), UI (0ev) | [phase] discovery (0ev), development (0ev), prod (0ev) | [task] memory (1ev), implement-projects-tab (0ev)
Last work (2026-03-18): There are some error - on the first load, it lookls like Backend is failing (after thay it succeed). I have tried to run the memory - and ther ewas an