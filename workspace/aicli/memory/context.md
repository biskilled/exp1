[PROJECT CONTEXT: aicli]
aicli is a multi-interface LLM memory and workflow platform enabling Claude Code, CLI, Cursor, and web UI to share unified project context. The system implements a 3-layer memory architecture (raw captures → structured facts → work items), role-based YAML-configured agent pipelines, and semantic search via pgvector; currently focused on finalizing agent role UI with base snapshot restoration and preparing pipeline execution orchestration.
Stack: language_cli=Python 3.12, cli_framework=prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: MCP Catalog wiring verification: confirmed all 10 MCPs fully implemented with activate/deactivate/edit modals, role-usage chips, and dedicated view under main left nav Workflows section, Role UI styling: colored pill backgrounds for Provider+Model matching Settings design; full background shapes for model display; status pills (BASED/UPDATED/EXTERNAL) with green/orange/amber colors; removed redundant small inline restore buttons, System prompts cleanup: soft-deleted 28 legacy system roles; retained only 3 canonical presets (Coding—General, Design & Planning, Review & Quality); all 10 roles correctly mapped to canonical presets; reset-to-defaults fully functional
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts via /memory POST + Haiku synthesis) → work items (mem_work_items with wi_parent_id hierarchy); ONLY approved work items (UC/FE/BU/TA prefix) embed to pgvector; Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; CLAUDE.md, CODE.md, PROJECT.md all regenerated from single JSON state; Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status TEXT (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id progression: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-04-29): Looks good. why only one is marked as BASED and all the rest ?