[PROJECT CONTEXT: aicli]
aicli is a multi-LLM memory system that gives Claude Code, the CLI, Cursor, and web UI a shared project knowledge base with unified work item tracking, role-based agent execution, and pipeline DAG orchestration. Currently focused on completing the pipeline execution UI with checkpoint controls, document/file input handling, and dashboard analytics restoration while maintaining a 3-layer memory architecture (raw captures → artifacts → approved work items).
Stack: language_cli=Python 3.12, cli_framework=prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Pipeline execution UI: Pipelines tab (◈ icon) shows full builder (graph_workflow.js) with node configuration panel; left panel lists all activated pipelines/roles; right detail panel shows pipeline flow visualization and per-stage node properties (name, provider, model, temperature, max_iterations); POST /agents/pipeline-runs triggers async DAG execution, Execute bar unified input: output folder combobox (default = pipeline name) + searchable project docs dropdown + multi-file upload in same row; files shown as removable chips above textarea for prompt/context input; all inputs support multiple selections and can be combined, Dashboard restoration: Dashboard tab (◫ icon) restored with Mirror Data cards (commits, prompts, items, messages—total + 24h), approved vs waiting-to-approve work items/use cases, total embeddings, Pipeline Runs health tiles showing last 24h execution status
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts via /memory POST + Haiku synthesis) → approved work items (mem_work_items with wi_parent_id hierarchy); ONLY approved work items (UC/FE/BU/TA prefix) embed to pgvector; Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; CLAUDE.md, CODE.md, PROJECT.md all regenerated from single JSON state; Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status TEXT (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id progression: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-04-30): I dont see any  UI update, when I am trying to load new document - it is not working. dropbox - shows all files, but I cannot select  as an input . Al