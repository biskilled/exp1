[PROJECT CONTEXT: aicli]
aicli is a unified LLM project memory system that keeps Claude Code, Cursor, the aicli CLI, and the web UI in sync across a shared knowledge base. It features a 3-layer memory architecture (raw captures → structured facts → work items), a multi-LLM workflow DAG for design/review/develop/test, semantic search via pgvector, and a fully YAML-configurable agent/pipeline system with role snapshots and tool/MCP management.
Stack: language_cli=Python 3.12, cli_framework=prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: MCP Catalog full wiring: verify all 10 MCPs display correctly with activate/deactivate/edit modals and role-usage chips in UI, Retry configuration migration: move retry logic from agent roles to pipeline YAML stage definitions; wire up pipeline UI to create/edit pipelines with stage-level retry policies, System Prompts UI redesign for Pipeline/MCP Catalogs: apply same design pattern (collapsible panels, preview, edit, reset) to Pipeline editor and MCP Catalog as was done for Agent Roles
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts via /memory POST + Haiku synthesis) → work items (mem_work_items with wi_parent_id hierarchy); ONLY approved work items (UC/FE/BU/TA prefix) embed to pgvector; Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; CLAUDE.md, CODE.md, PROJECT.md all regenerated from single JSON state; Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status TEXT (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id progression: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-04-29): Can you add the Model as well . Also reset to orinigla suppose to reset the role to the base file (include attached system prompts, tools and mcp) . I