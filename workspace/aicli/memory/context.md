[PROJECT CONTEXT: aicli]
aicli is a unified LLM memory layer that gives Claude Code, the aicli CLI, Cursor, and web UI a shared project context. It captures commits, work items, and sessions in a memory architecture (raw captures → structured artifacts → embeddings) and surfaces them via semantic search, MCP tools, and auto-generated files (PROJECT.md, CODE.md, CLAUDE.md). The system uses a 4-agent pipeline (PM→Architect→Developer→Reviewer) for approved work items and runs on PostgreSQL + FastAPI backend with Electron + Vite desktop UI.
Stack: cli=Python 3.12 + prompt_toolkit + rich, backend=FastAPI + uvicorn + python-jose + bcrypt + psycopg2, frontend=Vanilla JS + Electron + Vite, ui_components=xterm.js + Monaco editor + Cytoscape.js, storage=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Code quality audit (sessions 37–40): verified all modules <1500 lines, SQL queries optimized (no N+1), recursive CTEs bounded depth<20, no dead code; token counting switched to len(text)//4; _load_context() split into _query_db_into_ctx() and _parse_project_md()., Performance investigation: PROJECT.md load times >60s traced to get_project_context() aggregation inefficiency; refactored with separate DB query and markdown parsing paths to reduce round-trips., Memory file convergence: unified get_project_context() path for both /memory POST and commit/work-item triggers; removed duplicate render paths (render_root_claude_md) so all files use same DB source.
Decisions: Memory architecture: 3 layers — raw captures (mem_mrr_*), structured artifacts (mem_ai_project_facts, mem_work_items), and embeddings (pgvector 1536-dim for approved work items only).; Work items: unified hierarchy with wi_parent_id linking features/bugs/tasks to use_case parents; approved items (wi_id: UC/FE/BU/TA) embed and trigger 4-agent pipeline; unapproved drafts (AI prefix) do not embed.; Project context generation: ONLY POST /memory endpoint writes project_state.json (from get_project_context DB queries + Haiku synthesis); this drives all 5 memory files (CLAUDE.md, CODE.md, PROJECT.md, cursor/rules, api/).
Last work (2026-04-27): I would like to run the last prompt again -  This time please go over on all latest changes (in the last 10-15 prompts) check if code is optimised and