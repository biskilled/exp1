[PROJECT CONTEXT: aicli]
aicli is a shared LLM memory system that enables Claude Code, the CLI, Cursor, and a web UI to access the same project context via a centralized PostgreSQL database. It includes a multi-agent async DAG pipeline executor, semantic search over git commits and prompts via pgvector, YAML-based agent roles/pipelines with runtime overrides, and MCP server tools for memory queries. The UI has recently stabilized work item verdict zones with per-item scoring, per-stage execution logs, resizable pipeline run panels, and MD report generation.
Stack: language_cli=Python 3.12, cli_framework=prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Loop detection fix: agent.py _detect_loop() now checks for identical tool name AND input across 3+ consecutive steps, allowing safe iteration over multiple items with the same tool without triggering false loop detection, Code Reviewer role refinement: validates each work item against acceptance_criteria, outputs structured JSON with score (0-5) and reason per item to structured_out; ensures actionable feedback and per-item scoring in verdict zone, Per-stage execution logs UI: removed global 'Execution Log' toggle; each stage now has collapsible <details> with logs appearing only under that stage; stage summaries display verdict/duration/cost first
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts) → approved work items (mem_work_items); ONLY approved items (UC/FE/BU/TA prefix) embed to pgvector; re-embedding automatic via update() when item fields change; Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; CLAUDE.md, CODE.md, PROJECT.md all regenerated from single JSON state; Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status TEXT (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id progression: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-05-03): I still reciave the same response - Thought: No memory found for any of the work items. Let me look at the project structure and the actual diff more 