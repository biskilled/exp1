[PROJECT CONTEXT: aicli]
aicli is a shared LLM memory system that enables Claude Code, CLI, Cursor, and a web UI to seamlessly share project context via a PostgreSQL+pgvector backend. It captures raw events (commits, prompts, items) into a 3-layer architecture, synthesizes them into project facts and work items, and exposes them through an MCP server and REST API; the system uses tree-sitter hotspot scoring, async DAG workflows, and multi-provider LLM agents to automate code review and feature development pipelines. Currently addressing DB schema cleanup (removing deprecated lifecycle/event_type columns), fixing startup race conditions in project selector, and optimizing PROJECT.md load performance.
Stack: language_cli=Python 3.12, cli_framework=prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Fix undefined column errors: route_entities (line 359: t.lifecycle) and route_history (line 228: event_type) — columns removed in migration m080 but route code not yet updated, Remove lifecycle tags from Planner UI: 11 active drag-and-drop, category display, archive toggle, and tagging UI errors related to deprecated lifecycle field, Fix backend startup race condition: active project not displayed in project selector after startup; recent projects list missing aiCli project; likely init sequencing issue in project loader
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts via /memory POST + Haiku synthesis) → work items (mem_work_items with wi_parent_id hierarchy); ONLY approved work items (UC/FE/BU/TA prefix) embed to pgvector (1536-dim, text-embedding-3-small); Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; all 3 output files (CLAUDE.md, CODE.md, PROJECT.md) regenerated from single JSON state in _write_root_files_with_ctx(); Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status TEXT (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-04-29): When I am using the cluade sdk - do I need to provide tool ? is there is a set of tools that I know which one to use, or is it using what ever is need