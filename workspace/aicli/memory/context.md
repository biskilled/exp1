[PROJECT CONTEXT: aicli]
aicli gives every LLM the same shared project memory across Claude Code, the CLI, Cursor, and web UI — eliminating context copying and re-explanation of architecture. It uses a 3-layer memory architecture (raw captures → structured artifacts → work items), an async DAG pipeline (PM → Architect → Developer → Reviewer), and pgvector embeddings for semantic search, all backed by PostgreSQL and 14 MCP tools for unified project context access.
Stack: cli=Python 3.12 + prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small), database_client=psycopg2
In progress: Fix PROJECT.md file loading timeout (>60s) — PROJECT.md now read once in single pass but performance audit ongoing; may need database indices on project, wi_type, user_status, Fix undefined column errors in route_entities (line 359: t.lifecycle) and route_history (line 228: event_type) — these columns were removed in migration m080 but route code not yet updated, Fix commit sync batch upsert error in /history/commits/sync API (execute_values parameter mismatch) and tag counter not updating in Planner UI when tags added/removed
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts via /memory POST + Haiku synthesis) → work items (mem_work_items with wi_parent_id hierarchy); ONLY approved work items (UC/FE/BU/TA prefix) embed to pgvector (1536-dim, text-embedding-3-small); Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; all 3 output files (CLAUDE.md, CODE.md, PROJECT.md) regenerated from single JSON state in _write_root_files_with_ctx(); Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-04-29): you mention planer is not working well as fe matched TA401 as planer inseaed of database - is it possilbe to imprve accureny ? Also you mention delive