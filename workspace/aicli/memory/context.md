[PROJECT CONTEXT: aicli]
aicli is an AI-native CLI and desktop app that gives every LLM (Claude, GPT, DeepSeek, etc.) shared project memory. Users interact via CLI or Electron UI; all sessions feed a unified knowledge base (PostgreSQL + pgvector) that syncs context across multiple AI platforms. The system includes a DAG-based multi-agent workflow engine, role-based agent library with YAML-defined identity/behavior, and a 3-layer memory architecture (raw captures → structured artifacts → approved work items). Current focus: pipeline execution UI with unified execute bar, checkpoint controls, and role library direct execution.
Stack: language_cli=Python 3.12, cli_framework=prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Execute bar UI refinement: output folder, project docs dropdown, multi-file upload unified in single row; dropdown styling with explicit background/z-index; layout alignment with checkpoint control checkboxes (max_retry, stateless, continue-on-fail, approval_gate) positioned below node diagram, CLI sessions visibility in Chat: backend SQL fix to read src column instead of tags->>'source'; CLI chat history now properly grouped by session in History view with prompts/responses, Dashboard restoration: Mirror Data cards (commits, prompts, items, messages—total + 24h), Work Items / Use Cases (approved vs waiting-to-approve, total embeddings), Pipeline Runs health tiles
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts via /memory POST + Haiku synthesis) → approved work items (mem_work_items with wi_parent_id hierarchy); ONLY approved work items (UC/FE/BU/TA prefix) embed to pgvector; Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; CLAUDE.md, CODE.md, PROJECT.md all regenerated from single JSON state; Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status TEXT (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id progression: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-04-30): I am looking at my chat history - it used to be goupeed by session. I do see all prmotps and llm response in history. but in the chat section - all I 