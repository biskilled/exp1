[PROJECT CONTEXT: aicli]
aicli is a shared LLM memory system enabling Claude Code, CLI, Cursor, and web UI to access unified project context. It captures commits, work items, and prompts into PostgreSQL, synthesizes project state via Haiku, and renders it across CLAUDE.md, CODE.md, PROJECT.md with semantic search over approved work items.
Stack: cli=Python 3.12 + prompt_toolkit + rich, backend=FastAPI + uvicorn + python-jose + bcrypt + psycopg2, frontend=Vanilla JS + Electron + Vite, ui_components=xterm.js + Monaco editor + Cytoscape.js, storage=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Code audit cycle: fresh aicli_memory.md audit in progress reviewing all changes from last 10-15 prompts for optimization, SQL efficiency, code duplication, and file length reasonableness., Date cascade validation: _apply_date_rules() now prevents re-parenting work items to use cases with earlier due_dates; added depth check when only wi_parent_id changes., Database query optimization: eliminated N+1 hotspot existence checks via batch WHERE name = ANY(%s) in memory_files.py; _update_item_tags refactored to use executemany instead of N separate execute calls.
Decisions: Memory architecture: 3 layers — raw captures (mem_mrr_*), structured artifacts (mem_ai_project_facts), and work items (mem_work_items) with pgvector embeddings for approved items only (1536-dim, text-embedding-3-small).; Work items: unified hierarchy with wi_parent_id linking features/bugs/tasks to use_case parents; approved items (wi_id: UC/FE/BU/TA) embed and trigger 4-agent pipeline; unapproved drafts (AI prefix) never embed.; File generation: /memory POST endpoint is the ONLY writer to project_state.json via get_project_context() + Haiku synthesis; this single JSON drives all 5 output files (CLAUDE.md, CODE.md, PROJECT.md, cursor/rules, api/).
Last work (2026-04-27): Can i use  the  memory layer in production? did you optimise code as  possilbe, clean unsud data? please answer the following - 1. update memory  file