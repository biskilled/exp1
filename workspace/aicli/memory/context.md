[PROJECT CONTEXT: aicli]
AI-powered development CLI with multi-provider support
Stack: cli=Python 3.12 + prompt_toolkit + rich, backend=FastAPI + uvicorn + python-jose + bcrypt + psycopg2, frontend=Vanilla JS + Electron + Vite dev server, ui_components=xterm.js + Monaco editor + Cytoscape.js + cytoscape-dagre, storage=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: MCP tool restoration: verify route_entities.py deletion impact; ensure all work item tools (create_entity, sync_github_issues, list_work_items) route to correct endpoints, Backend prompt consolidation: verify all LLM prompts use YAML templates under backend/memory/mem_*.yaml; ensure project_state.json populates correct context for /memory, Frontend hardcoded string removal: validate all localhost references removed; confirm dynamic config loading from aicli.yaml for backend URLs
Decisions: Workspace structure simplified: aicli/cli/{claude,mcp}/ for hooks/configs; pipelines/{prompts,samples}/ for workflows; documents/ for project files; state/ for runtime state; .ai/ and _system/ folders removed; all generated files now under aicli/cli/ with templates in backend/memory/templates/; Dual storage: PostgreSQL 15+ with pgvector (1536-dim) for semantic search; unified mem_mrr_* and mem_work_items tables
Last work (2026-04-26): Just to be cleared -The 4 agent pipeline suppose to work on approved open items under an approved use case