[PROJECT CONTEXT: aicli]
aicli is a multi-LLM project memory system that gives Claude, ChatGPT, DeepSeek, and other AI assistants the same project context across CLI, web UI, and editor integrations. It combines a 3-layer memory architecture (raw captures → structured artifacts → approved work items), async pipeline DAG execution with role-based agents, and pgvector semantic search. Current focus is stabilizing pipeline execution logs, embedding scope validation, and document management UI.
Stack: language_cli=Python 3.12, cli_framework=prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Execution logs visibility: Execution Log section now open by default (<details open>) displaying ReAct iterations (tool steps, observations) instead of just start/done markers, Pipeline list validation: All 8 YAML pipelines validated (standard, build_feature, etc.) using correct role references; pipeline display now uses listPipelines() API consistently without duplicate 'Recent Run' section, Embedding scope expansion: Architect agent now checks memory for sorted items; embedding only on closed/approved items; CODE.md requires embedding scope update per memory architecture
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts via /memory POST + Haiku synthesis) → approved work items (mem_work_items with wi_parent_id hierarchy); ONLY approved items (UC/FE/BU/TA prefix) embed to pgvector; Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; CLAUDE.md, CODE.md, PROJECT.md all regenerated from single JSON state; Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status TEXT (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id progression: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-05-02): When I look at the pipelines - I dont see this pipeline. can you confirm that all pipeline are using proper yaml file , and existing roles as it suppo