[PROJECT CONTEXT: aicli]
aicli is a shared LLM memory system that enables Claude Code, the aicli CLI, Cursor, and a web UI to all access the same project context—codebase structure, decisions, feature history, and work items—without manual context copying. A PostgreSQL + pgvector backend, Python CLI, and Electron desktop app form the core; a FastAPI server runs agents and work item classification; and an MCP server provides 14 tools for Claude Code integration.
Stack: cli=Python 3.12 + prompt_toolkit + rich, backend=FastAPI + uvicorn + python-jose + bcrypt + psycopg2, frontend=Vanilla JS + Electron + Vite, ui_components=xterm.js + Monaco editor + Cytoscape.js, storage=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Fix PROJECT.md file loading timeout (>60s) and backend startup race condition — likely caused by missing database indices or N+1 queries in project context loading, Fix 11 active bugs in UI (category display, drag-and-drop, archive toggles), backend (undefined column errors in routes, startup race), and database schema persistence, Fix commit sync batch upsert error in /history/commits/sync API and tag counter not updating in Planner UI
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts) → work items (mem_work_items with pgvector ONLY for approved UC/FE/BU/TA items); Single source of truth: /memory POST endpoint is the ONLY writer to project_state.json via get_project_context() + Haiku synthesis; all 3 output files (CLAUDE.md, CODE.md, PROJECT.md) regenerated from single JSON; Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement) and wi_parent_id linking children to use_case parents; wi_id: AI0001 (draft) → UC/FE/BU/TA0001 (approved); only approved items embed and trigger 4-agent pipeline
Last work (2026-04-28): do i pay claude sdk by token useage same sa cluade api ?