[PROJECT CONTEXT: aicli]
aicli gives every LLM the same project memory by syncing shared knowledge across Claude Code, the CLI, Cursor, and a web UI. It features a 3-layer memory architecture (raw captures → structured artifacts → approved work items), a unified agent role system with YAML-based configuration seeding to PostgreSQL, and a 4-agent async DAG pipeline (PM → Architect → Developer → Reviewer) that executes on approved backlog items. Currently focused on completing pipeline execution UI, role activation/versioning controls, and visual design consolidation across the Roles & Pipelines settings panel.
Stack: language_cli=Python 3.12, cli_framework=prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Pipeline execution framework: GET /agent-roles/pipelines/{name} returns full pipeline config with stage role details and default_temperature; POST /agents/pipeline-runs starts async DAG execution with role overrides (provider/model/temperature); UI execution panel displays stage outputs with async coordination, Role activation in Workflows Settings: Settings/Workflow renamed to Roles & Pipelines with dual-pane dual-column view; left pane shows all roles/pipelines with activation checkboxes; only activated items appear in role/pipeline tabs; reset-to-base and role-versioning via mng_agent_role_versions, UI visual design consolidation: role pills styled with provider color backgrounds (matching Settings Agent Role design); status badges (BASED green, UPDATED orange, EXTERNAL amber); all buttons and model displays have background fills for clarity
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts via /memory POST + Haiku synthesis) → approved work items (mem_work_items with wi_parent_id hierarchy); ONLY approved work items (UC/FE/BU/TA prefix) embed to pgvector; Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; CLAUDE.md, CODE.md, PROJECT.md all regenerated from single JSON state; Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status TEXT (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id progression: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-04-30): I dont see anything. is it under pipeline section ?