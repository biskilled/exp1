[PROJECT CONTEXT: aicli]
aicli is a multi-LLM project memory platform that gives Claude Code, CLI, Cursor, and web UI shared access to the same codebase context, decisions, and work items. It features a 3-layer memory architecture (raw captures → AI-synthesized facts → approved work items), a role-based agent execution model with YAML-defined roles/pipelines, and integrated 10-tool MCP server. Currently under active development with focus on unified execute bar UI, pipeline/role direct execution parity, dashboard restoration with work item approval metrics, and bug fixes in chat history grouping and database column cleanup.
Stack: language_cli=Python 3.12, cli_framework=prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Dashboard restoration: Mirror Data cards (commits, prompts, items, messages—total + 24h), approved vs waiting-to-approve work items/use cases, total embeddings, Pipeline Runs health tiles showing last 24h execution status, Execute bar unified input styling: output folder, docs dropdown, files multi-select aligned in single row with consistent height/padding; dropdown styled with explicit background, shadow, z-index to float above prompts, Chat history by session: CLI sessions now visible in Chat tab (fixed source column mapping: hook strips source from tags, stores in src column); session grouping restored; execution logs below in history panel
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts via /memory POST + Haiku synthesis) → approved work items (mem_work_items with wi_parent_id hierarchy); ONLY approved work items (UC/FE/BU/TA prefix) embed to pgvector; Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; CLAUDE.md, CODE.md, PROJECT.md all regenerated from single JSON state; Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status TEXT (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id progression: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-04-30): I am looking at my chat history - it used to be goupeed by session. I do see all prmotps and llm response in history. but in the chat section - all I 