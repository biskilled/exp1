[PROJECT CONTEXT: aicli]
aicli is a unified LLM memory platform that gives Claude, ChatGPT, Cursor, and other AI tools shared project context—same codebase knowledge, decisions, and feature history across all interfaces. Built with FastAPI + PostgreSQL backend, Electron + Vanilla JS frontend, supporting multi-LLM workflows (Claude/OpenAI/DeepSeek/Gemini/Grok), role-based agent execution via async DAG pipelines, and MCP server integration.
Stack: language_cli=Python 3.12, cli_framework=prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Pipeline design initiation: planning per-stage parameter overrides (temperature/top_p), DAG node creation UI wired to existing roles, pipeline execution wiring via asyncio.gather, Settings/Workflow consolidation: renamed to 'Roles & Pipelines' with checkbox-based availability toggle, pipeline enable-only-if-all-roles-available validation implemented, Role UI finalization: base_snapshot JSONB storage complete, version history browsing wired, BASED/UPDATED/EXTERNAL status badges (green/orange/amber) displayed per role, provider-model coupling on UI change enforced
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts via /memory POST + Haiku synthesis) → approved work items (mem_work_items with wi_parent_id hierarchy); ONLY approved work items (UC/FE/BU/TA prefix) embed to pgvector; Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; CLAUDE.md, CODE.md, PROJECT.md all regenerated from single JSON state; Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status TEXT (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id progression: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-04-29): I would like to start to work on pipelines section.   pipeline and roles can be executed from thus tab (only approved pipeline and approved roles)  pi