[PROJECT CONTEXT: aicli]
aicli gives every LLM the same project memory by storing shared context in PostgreSQL + pgvector, so Claude Code, aicli CLI, Cursor, and web UI all access identical project state, work items, and code analysis. The project uses a 3-layer memory architecture (raw captures → structured artifacts → work items), a multi-agent DAG pipeline (PM → Architect → Developer → Reviewer), and YAML-based role/prompt configuration to eliminate context copying across tools.
Stack: language_cli=Python 3.12, cli_framework=prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Fix backend startup race condition and project selector: active project not displayed in project selector after startup; recent projects list missing aiCli project; likely init sequencing issue in project loader or database connection timing, Fix undefined column errors in route_entities and route_history: columns removed in migration m080 (lifecycle, event_type) but route code not yet updated — causing UndefinedColumn psycopg2 errors on startup; needs immediate column reference audit, Remove lifecycle tags and drag-and-drop issues from Planner UI: lifecycle field deprecated but 11 active references remain in drag-and-drop, category display, archive toggle, and tagging UI; also fix [object object] display bug in tag additions
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts via /memory POST + Haiku synthesis) → work items (mem_work_items with wi_parent_id hierarchy); ONLY approved work items (UC/FE/BU/TA prefix) embed to pgvector (1536-dim, text-embedding-3-small); Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; all 3 output files (CLAUDE.md, CODE.md, PROJECT.md) regenerated from single JSON state in _write_root_files_with_ctx(); Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status TEXT (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-04-29): please place all yaml confing under memroy in yaml_config folder . also add misc.yaml and rename that to feature_detect as this is more suitable name.