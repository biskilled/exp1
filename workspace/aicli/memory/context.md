[PROJECT CONTEXT: aicli]
aicli is a unified LLM memory layer that allows Claude Code, the CLI, Cursor, and the web UI to share the same project context, backlog, and work items. It provides a PostgreSQL-backed memory architecture (raw captures, structured artifacts, embeddings), semantic search over approved work items, a 4-agent DAG pipeline for decomposing features, and auto-commit hooks to keep memory synchronized across all tools.
Stack: cli=Python 3.12 + prompt_toolkit + rich, backend=FastAPI + uvicorn + python-jose + bcrypt + psycopg2, frontend=Vanilla JS + Electron + Vite, ui_components=xterm.js + Monaco editor + Cytoscape.js, storage=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Code quality audit (sessions 37–40): verified all modules <1500 lines, SQL queries optimized (no N+1), recursive CTEs bounded depth<20, token counting switched to len(text)//4; _load_context() split into _query_db_into_ctx() and _parse_project_md()., Memory file convergence: unified get_project_context() path for both /memory POST and commit/work-item triggers; all 5 files now use same DB source and Haiku synthesis; removed duplicate render paths., Work item auto-closure on commits: regex matcher ('fixes BU0012', 'closes FE0001', 'resolve TA0003') auto-sets score_status=5, score_importance=5; requires user approval to mark user_status='done'.
Decisions: Memory architecture: 3 layers — raw captures (mem_mrr_*), structured artifacts (mem_ai_project_facts), and work items (mem_work_items) with pgvector embeddings for approved items only (1536-dim, text-embedding-3-small).; Work items: unified hierarchy with wi_parent_id linking features/bugs/tasks to use_case parents; approved items (wi_id: UC/FE/BU/TA) embed and trigger 4-agent pipeline; unapproved drafts (AI prefix) never embed or run pipeline.; File generation: /memory POST endpoint is the ONLY writer to project_state.json via get_project_context() + Haiku synthesis; this single JSON drives all 5 output files (CLAUDE.md, CODE.md, PROJECT.md, cursor/rules, api/).
Last work (2026-04-27): I would like to run the last prompt again -  This time please go over on all latest changes (in the last 10-15 prompts) check if code is optimised and