[PROJECT CONTEXT: aicli]
aicli gives every LLM the same project memory across Claude Code, CLI, Cursor, and web UI by storing shared context in PostgreSQL + pgvector. The system uses a 3-layer memory architecture (raw captures → structured artifacts → work items), a 4-agent pipeline (PM → Architect → Developer → Reviewer), and an MCP server with 14 tools. Currently fixing production-blocking bugs in PROJECT.md loading timeout, undefined column errors, and UI state sync issues.
Stack: cli=Python 3.12 + prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small), database_client=psycopg2
In progress: Fix PROJECT.md file loading timeout (>60s) — likely N+1 queries in project context loading or missing database indices; PROJECT.md now read once in single pass but performance audit ongoing, Fix undefined column errors in route_entities (line 359) and route_history (line 228) referencing removed lifecycle field from mem_ai_events migration m080, Fix commit sync batch upsert error in /history/commits/sync API (execute_values parameter mismatch) and tag counter not updating in Planner UI when tags added/removed
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts via /memory POST + Haiku) → work items (mem_work_items with wi_parent_id hierarchy); ONLY approved work items (UC/FE/BU/TA prefix) embed to pgvector; Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; all 3 output files (CLAUDE.md, CODE.md, PROJECT.md) regenerated from single JSON state; Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-04-28): you have see all tiems in work tiem table. can you check how much data you have for each one of them and map sujjested pipeline ?