[PROJECT CONTEXT: aicli]
AgentDesk is a unified LLM memory and agentic workflow platform that allows Claude, Cursor, and CLI tools to share project context seamlessly. It features a 3-layer memory architecture (raw captures → structured facts → approved work items), semantic search via pgvector, multi-LLM pipeline execution (Claude/OpenAI/DeepSeek/Gemini/Grok), and YAML-based role/pipeline configuration. Currently in active development with core pipelines, work item classification, and execution logs working; UI recently refactored with resizable panels, per-role log toggles, and verdict scoring.
Stack: language_cli=Python 3.12, cli_framework=prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: AgentDesk branding: Updated UI (index.html, main.js, package.json) to replace 'aicli' with 'AgentDesk'; logo image support added with 22×22px circular styling in titlebar, Pipeline execution flow: Fully implemented 3-point execution (chat /pipeline, pipeline section, use case/item runners); all 9 steps verified including planning_out/planning_tokens/planning_cost_usd columns and approval flow, Verdict zone per-item scoring: items_reviewed table displaying FE####/BU####/etc with 0-5 scores and reasoning; structured_out JSON parsing extracts per-item verdict data from reviewer stage output
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts) → approved work items (mem_work_items); ONLY approved items (UC/FE/BU/TA prefix) embed to pgvector; re-embedding on item change automatic via update() → _embed_work_item(); Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; CLAUDE.md, CODE.md, PROJECT.md all regenerated from single JSON state; Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status TEXT (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id progression: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-05-03): I would like to call the app AgentDesk (not AiCli) - can you update that in the UI ? Also can you let me know which folder I can upload small image (c