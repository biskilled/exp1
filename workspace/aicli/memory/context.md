[PROJECT CONTEXT: aicli]
aicli is a multi-interface LLM memory system that gives Claude Code, the CLI, Cursor, and web UI shared project context. It captures commits, prompts, and work items into a PostgreSQL backend with pgvector embeddings (approved items only), renders 3 memory files (CLAUDE.md, CODE.md, PROJECT.md), and executes 4-agent workflows. Currently in production-ready state with 16 open bugs requiring fixes in UI rendering, backend startup timing, and route column definitions.
Stack: cli=Python 3.12 + prompt_toolkit + rich, backend=FastAPI + uvicorn + python-jose + bcrypt + psycopg2, frontend=Vanilla JS + Electron + Vite, ui_components=xterm.js + Monaco editor + Cytoscape.js, storage=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: 16 active bugs across in-progress/pending/review: UI category display, drag-and-drop, archive/unarchive toggles, undefined column errors in routes, backend startup race condition, PROJECT.md load timeout (>60s), MCP server fully operational: all 14 tools dispatched correctly via REST endpoints; Backend db_connected: true; ready for Claude Code sessions with shared project memory, Production readiness confirmed: memory files (CLAUDE.md, CODE.md, PROJECT.md) provide good project structure; work items/use cases function correctly; 4-agent pipeline ready
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts) → work items (mem_work_items with pgvector ONLY for approved UC/FE/BU/TA items); Single source of truth: /memory POST endpoint is the ONLY writer to project_state.json via get_project_context() + Haiku synthesis; all 3 output files (CLAUDE.md, CODE.md, PROJECT.md) regenerated from single JSON; Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement) and wi_parent_id linking children to use_case parents; wi_id: AI0001 (draft) → UC/FE/BU/TA0001 (approved); only approved items embed and trigger 4-agent pipeline
Last work (2026-04-28): Do I have to set up all the tools using claude sdk ? or there are tamplte ? when I am using claude -p - it is automaticly load relevant tools and use 