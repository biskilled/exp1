[PROJECT CONTEXT: aicli]
aicli is a multi-LLM project memory system enabling Claude, Cursor, and CLI to share unified codebase context, work items, and execution history. It combines a PostgreSQL backend with pgvector semantic search, a 3-layer memory architecture (raw captures → artifacts → approved work items), and agentic YAML-defined pipelines (Architect → Developer → Reviewer) triggered from use cases. Current focus: improving structured pipeline output (per-item scoring, markdown reports), optimizing role execution feedback (per-stage logs, approval gates), and refining the execution panel UX (resizable, editable paths).
Stack: language_cli=Python 3.12, cli_framework=prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Verdict zone per-item scores display: items_reviewed table showing FE####/BU####/etc with scores 0-5 and reasoning; working but LLM sometimes outputs 'Thought:' continuation instead of final JSON, preventing structured_out parsing, Approval/rejection UI flow: approval buttons now use window._ucApprovePipelineRun() instead of inline api reference (ReferenceError fixed); flows to backend PATCH /pipeline/{run_id}/approval endpoint, Pipeline report markdown generation: saving to workspace/{project}/documents/pipelines/{pipeline_name}/{ddmmyy_HHMM}_{uc_slug}.md using in-memory _stage_mem data collected during execution; folder and filename now editable via PATCH endpoint
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts) → approved work items (mem_work_items); ONLY approved items (UC/FE/BU/TA prefix) embed to pgvector; re-embedding on item change automatic via update() → _embed_work_item(); Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; CLAUDE.md, CODE.md, PROJECT.md all regenerated from single JSON state; Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status TEXT (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id progression: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-05-03): Ok. I do see now approval/reject - but there is an error - 2026-05-03 21:34:48 | ERROR    | routers.route_logs                  | [UI] http://localhos