[PROJECT CONTEXT: aicli]
aicli gives every LLM the same project memory by syncing codebase context, decisions, and feature history across Claude Code, the CLI, Cursor, and web UI. It uses a 3-layer memory architecture (raw captures → structured artifacts → approved work items), multi-LLM async DAG pipelines, pgvector semantic search, and role-based agents defined in YAML templates. Current focus: testing pipeline execution, fixing stage visibility/logging, cost calculation, and preparing for React refactor.
Stack: language_cli=Python 3.12, cli_framework=prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Pipeline execution testing & debugging: fixing ToolUseBlock attribute errors (getattr fallback), messages.N tool_result sequencing (Anthropic format), cost calculation for max_iterations path, and stage timeout issues (Architect stage taking 122K tokens / 103s), Execution history & logging UI: stage detail panels showing input/output snapshots, steps_json ReAct trace visualization, verdict banner with score_dots (●/○), session-level chat history grouped by source (CLI vs web), Stage visibility and rich output: rendering input_snapshot as text/file summary and output_snapshot artifacts (code changes, file outputs), collapsible ReAct steps panel per stage, duration/cost per-stage breakdown
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts via /memory POST + Haiku synthesis) → approved work items (mem_work_items with wi_parent_id hierarchy); ONLY approved items (UC/FE/BU/TA prefix) embed to pgvector; Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; CLAUDE.md, CODE.md, PROJECT.md all regenerated from single JSON state; Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status TEXT (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id progression: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-05-02): I am looking to the sample pipeline - and try to run a pipeline on use case  Issues - tokens per stage - Architect - 122K, Developer - 60k and revieer