[PROJECT CONTEXT: aicli]
aicli is a multi-LLM project memory system that unifies Claude Code, CLI, Cursor, and web UI through a shared PostgreSQL knowledge base. It features a 3-layer memory architecture (raw captures → structured artifacts → approved work items), role-based agent execution with DAG pipelines, semantic search via pgvector, and multi-user billing. Current focus is fixing pipeline/role execution UI, chat session visibility, and performance optimizations.
Stack: language_cli=Python 3.12, cli_framework=prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Pipeline/role execution UI: exec bar positioning fixed (moved inside gw-main, not inside canvas-area), doc picker redesigned as proper dropdown widget matching multi-select style, files shown as chips above textarea, output folder combobox with default name, Chat history and session grouping: Backend SQL reads src column for message sourcing, CLI chat sessions now visible in History grouped by session with prompts/responses properly displayed, session order and phase filtering working, Use Cases pipeline execution: Run button now shows dropdown of available pipelines, fixed dropdown positioning to not be clipped by card overflow, execution screen renders properly alongside use case details
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts via /memory POST + Haiku synthesis) → approved work items (mem_work_items with wi_parent_id hierarchy); ONLY approved work items (UC/FE/BU/TA prefix) embed to pgvector; Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; CLAUDE.md, CODE.md, PROJECT.md all regenerated from single JSON state; Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status TEXT (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id progression: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-04-30): I have started to test and got an error - ERROR: Stage 'architect' failed: 'ToolUseBlock' object has no attribute 'get' Also in becked I do see an err