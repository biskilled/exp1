[PROJECT CONTEXT: aicli]
aicli is a shared LLM memory platform that lets Claude Code, aicli CLI, Cursor, and a web UI all access the same project context—codebase, decisions, and feature history—without copy-pasting. It uses a 3-layer memory architecture (raw captures → structured facts → work items), a FastAPI+PostgreSQL backend with pgvector embeddings, and MCP tools for semantic search and work item routing. Currently undergoing bug fixes for UI field removal (role inputs/outputs/type), route query corrections post-migration, and pipeline routing accuracy improvements using full tech_stack context.
Stack: language_cli=Python 3.12, cli_framework=prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Improve delivery_type and pipeline routing accuracy: added full project tech_stack context (frontend/backend/database keywords) to Haiku classifier system prompt to reduce misclassification (e.g., TA4001 'planner' now correctly maps to database instead of frontend), Remove unused role UI fields: deleted inputs, outputs, and role_type columns from mng_agent_roles; removed corresponding validation and serialization code; updated role edit dialog to eliminate unused form fields, Fix undefined column errors: route_entities (line 359: t.lifecycle) and route_history (line 228: event_type) — columns removed in migration m080 but route code not yet updated
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts via /memory POST + Haiku synthesis) → work items (mem_work_items with wi_parent_id hierarchy); ONLY approved work items (UC/FE/BU/TA prefix) embed to pgvector (1536-dim, text-embedding-3-small); Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; all 3 output files (CLAUDE.md, CODE.md, PROJECT.md) regenerated from single JSON state in _write_root_files_with_ctx(); Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status TEXT (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-04-29): Please explain again why there is checkbox for ReAct in the role creation? when is it used ?