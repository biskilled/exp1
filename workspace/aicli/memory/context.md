[PROJECT CONTEXT: aicli]
aicli is a multi-agent LLM memory layer that unifies project context across Claude, Cursor, the CLI, and web UI. It captures commits, prompts, and work items into a PostgreSQL knowledge base with semantic search, auto-classifies tasks via Haiku, and renders shared memory files (CLAUDE.md, CODE.md, PROJECT.md) that every AI tool reads. The current work focuses on code optimization (bounded CTEs, batch queries, token counting) and ensuring the unified get_project_context() path is the single source for all file generation.
Stack: cli=Python 3.12 + prompt_toolkit + rich, backend=FastAPI + uvicorn + python-jose + bcrypt + psycopg2, frontend=Vanilla JS + Electron + Vite, ui_components=xterm.js + Monaco editor + Cytoscape.js, storage=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Code optimization audit: eliminated N+1 hotspot checks via batch WHERE queries, fixed token counting (len(text) // 4), optimized _update_item_tags to use executemany instead of N separate calls., Database schema & query performance: bounded all recursive CTEs (depth < 20), added date cascade validation in _apply_date_rules() to prevent re-parenting inconsistencies., Memory file rendering: refactored _load_context() to split DB queries (_query_db_into_ctx) and PROJECT.md parsing (_parse_project_md) to eliminate double file-read bug.
Decisions: Memory architecture: 3 layers — raw captures (mem_mrr_*), structured artifacts (mem_ai_project_facts), and work items (mem_work_items) with pgvector embeddings for approved items only (1536-dim, text-embedding-3-small).; Work items: unified hierarchy with wi_parent_id linking features/bugs/tasks to use_case parents; approved items (wi_id: UC/FE/BU/TA) embed and trigger 4-agent pipeline; unapproved drafts (AI prefix) never embed.; File generation: /memory POST endpoint is the ONLY writer to project_state.json via get_project_context() + Haiku synthesis; this single JSON drives all 5 output files (CLAUDE.md, CODE.md, PROJECT.md, cursor/rules, api/).
Last work (2026-04-27): I would like to run the last prompt again -  This time please go over on all latest changes (in the last 10-15 prompts) check if code is optimised and