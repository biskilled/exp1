[PROJECT CONTEXT: aicli]
aicli gives every LLM the same shared project memory across Claude Code, the aicli CLI, Cursor, and the web UI. It uses a 3-layer memory architecture (raw captures → structured facts → work items), role-based agents with configurable system prompts/tools/MCP, and a 4-agent async DAG pipeline (PM → Architect → Developer → Reviewer) for feature development. Currently completing agent role UI with reset-to-base functionality and temperature/top_p parameters; pipeline execution phase is next.
Stack: language_cli=Python 3.12, cli_framework=prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Role UI finalization: reset-to-base button, base_snapshot JSONB storage, version history browsing, BASED/UPDATED/EXTERNAL status badges (green/orange/amber), provider-model coupling on UI change, Settings/Workflow consolidation: renaming to 'Roles & Pipelines', checkbox-based availability toggle, pipeline enable-only-if-all-roles-available validation, Temperature/top_p role parameters: added to all 9 role YAML files, provider adapters updated to accept and pass through temperature, pipeline stage-level overrides designed (awaiting pipeline execution phase)
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts via /memory POST + Haiku synthesis) → work items (mem_work_items with wi_parent_id hierarchy); ONLY approved work items (UC/FE/BU/TA prefix) embed to pgvector; Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; CLAUDE.md, CODE.md, PROJECT.md all regenerated from single JSON state; Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status TEXT (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id progression: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-04-29): Just to confirm - the edit button in the setting/workflow - is it for editing the yaml file ? is it save as a new version and when user click on Reset