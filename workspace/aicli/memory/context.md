[PROJECT CONTEXT: aicli]
aicli is a multi-LLM agent orchestration platform with shared project memory that unifies Claude Code, aicli CLI, Cursor, and a web UI. Work items (use cases, features, bugs, tasks) flow through a 3-layer memory architecture into approved backlog, then execute through async DAG pipelines with role-based agents; currently focused on fixing pipeline execution (tool sequencing, cost calculation, rich stage logging) and improving UI responsiveness through in-place DOM updates.
Stack: language_cli=Python 3.12, cli_framework=prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Rich stage details UI: rendering input_snapshot as text/file summary and output_snapshot artifacts (code changes, file outputs), collapsible ReAct steps panel per stage (steps_json visualization), duration/cost per-stage breakdown with score_dots (●/○ indicators 0-5), Pipeline report generation: all pipeline run results saved as markdown files under pipeline/ folder (not documents/); reports generated on completion with summary of stage verdicts, tokens, costs, and artifacts, Work Items tab visibility fix: approved use cases not loading due to empty cache check; added cache freshness validation to _ucCacheData.length > 0
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts via /memory POST + Haiku synthesis) → approved work items (mem_work_items with wi_parent_id hierarchy); ONLY approved items (UC/FE/BU/TA prefix) embed to pgvector; Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; CLAUDE.md, CODE.md, PROJECT.md all regenerated from single JSON state; Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status TEXT (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id progression: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-05-02): I cant see the approved use cases, can tou check that it is loaind properly.  About saving the md file for pipeline - all pipeline result suppose to s