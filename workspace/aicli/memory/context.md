[PROJECT CONTEXT: aicli]
aicli is a multi-LLM workspace that gives every AI (Claude Code, CLI, Cursor, web UI) the same shared project memory. It features a 3-layer memory architecture (raw captures → structured facts → approved work items), YAML-defined agent roles with tool/MCP bundles, async DAG pipelines for multi-stage workflows, and semantic search via pgvector. Currently: pipeline execution UI is functional with proper layout and dropdown selectors for document/file input; CLI sessions now visible in chat history; Settings → Roles & Pipelines uses in-place DOM updates instead of full reloads; dashboard is being restored with usage counters and work item metrics.
Stack: language_cli=Python 3.12, cli_framework=prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Pipeline execution visual improvements: exec bar now positioned correctly below node diagram (not clipped by overflow:hidden); file/document picker dropdown styled with proper z-index and dark background to prevent bleeding into prompt textarea, Chat history restoration: CLI sessions now visible in Chat tab (fixed source field detection—backend strips 'source' tag and stores in 'src' column); session grouping and per-session prompt/response pairing restored, Use case pipeline execution: Use cases section now shows run button with dropdown of available pipelines (filtered by mode_use_case=true); item-level pipeline execution also functional with dropdown selector
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts via /memory POST + Haiku synthesis) → approved work items (mem_work_items with wi_parent_id hierarchy); ONLY approved work items (UC/FE/BU/TA prefix) embed to pgvector; Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; CLAUDE.md, CODE.md, PROJECT.md all regenerated from single JSON state; Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status TEXT (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id progression: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-04-30): I continue to check the pipeline -  I did get an error - ERROR: Stage 'architect' failed: Error code: 400 - {'type': 'error', 'error': {'type': 'inval