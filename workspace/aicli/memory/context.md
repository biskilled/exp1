[PROJECT CONTEXT: aicli]
aicli gives every LLM the same project memory—Claude Code, CLI, Cursor, and web UI all read from a unified PostgreSQL + pgvector backend. Users classify work items into a 3-layer memory architecture (raw captures → structured artifacts → approved items), route them through multi-agent DAG pipelines (PM → Architect → Developer → Reviewer), and maintain shared context across all AI tools via /memory endpoint and project_state.json. Currently fixing DB schema mismatches, optimizing file loading performance (>60s timeout), and improving pipeline routing accuracy using tech_stack signals.
Stack: language_cli=Python 3.12, cli_framework=prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Fix undefined column errors in route_entities (line 359: t.lifecycle) and route_history (line 228: event_type) — columns removed in migration m080 but route code not yet updated, Remove lifecycle tags from Planner UI — 11 active drag-and-drop, category display, archive toggles, and tagging UI errors remaining, Improve delivery_type and pipeline routing accuracy using full project tech_stack context and file coupling signals to reduce classifier mismatches
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts via /memory POST + Haiku synthesis) → work items (mem_work_items with wi_parent_id hierarchy); ONLY approved work items (UC/FE/BU/TA prefix) embed to pgvector (1536-dim, text-embedding-3-small); Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; all 3 output files (CLAUDE.md, CODE.md, PROJECT.md) regenerated from single JSON state in _write_root_files_with_ctx(); Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status TEXT (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-04-29): When I am usign the UI there are some issues. when I change the Provider, model is not updated (showing all avilable model for the new provider) . Rol