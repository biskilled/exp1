[PROJECT CONTEXT: aicli]
aicli gives every LLM the same shared project memory so users can switch between Claude Code, the aicli CLI, Cursor, or web UI and the AI picks up exactly where they left off—same codebase context, same decisions, same feature history. The system uses a 3-layer memory architecture (raw captures → structured artifacts → work items), a 4-agent ReAct pipeline (PM → Architect → Developer → Reviewer), and pgvector-backed semantic search, with all configuration consolidated into role and pipeline YAML files. Current development focuses on fixing database column errors, startup race conditions, UI rendering bugs, and performance timeouts.
Stack: language_cli=Python 3.12, cli_framework=prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: Fix undefined column errors in route_entities and route_history: columns removed in migration m080 (lifecycle, event_type) but route code not yet updated; causing UndefinedColumn psycopg2 errors, Fix backend startup race condition and project selector: active project not displaying in selector after startup; recent projects list missing aiCli project; init sequencing issue in project loader, Remove lifecycle tags and drag-and-drop issues from Planner UI: deprecated lifecycle field still active in drag-and-drop, category display, tagging; also fix [object object] display bug in tag additions
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts via /memory POST + Haiku synthesis) → work items (mem_work_items with wi_parent_id hierarchy); ONLY approved work items (UC/FE/BU/TA prefix) embed to pgvector; Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; CLAUDE.md, CODE.md, PROJECT.md all regenerated from single JSON state; Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status TEXT (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id progression: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-04-29): please do option A. user can update roles if they want , so it is enough.ALso rename all roles yaml to atart with prefix role_ and all piepline yaml w