[PROJECT CONTEXT: aicli]
AgentDesk is a shared LLM memory platform that enables Claude Code, Cursor, CLI, and web UI to access the same project context, decision history, and work items. The system uses a 3-layer memory architecture (raw captures → structured artifacts → approved work items), multi-LLM async DAG pipelines (Architect → Developer → Reviewer), and YAML-based role/pipeline configuration. Currently in active development with full pipeline execution, verdict scoring, and branding updates to AgentDesk.
Stack: language_cli=Python 3.12, cli_framework=prompt_toolkit + rich, backend_framework=FastAPI + uvicorn, backend_auth=JWT (python-jose + bcrypt) + DEV_MODE toggle, database=PostgreSQL 15+ with pgvector (1536-dim, text-embedding-3-small)
In progress: AgentDesk branding: Rebranded all UI (index.html, main.js, package.json, titlebar) from 'aicli' to 'AgentDesk'; logo image support added (22×22px circular styling in titlebar); awaiting logo.jpg file upload, Pipeline execution full flow: All 9 steps verified — chat /pipeline, pipeline section, use case/item runners fully functional; planning_out/planning_tokens/planning_cost_usd columns and approval flow working end-to-end, Verdict zone per-item scoring: items_reviewed table displaying FE####/BU####/etc with 0-5 scores and reasoning; structured_out JSON parsing extracts per-item verdict data from reviewer stage output
Decisions: Memory 3-layer architecture: raw captures (mem_mrr_* tables) → structured artifacts (mem_ai_project_facts) → approved work items (mem_work_items); ONLY approved items (UC/FE/BU/TA prefix) embed to pgvector; re-embedding on item change automatic via update() → _embed_work_item(); Single source of truth: /memory POST endpoint is ONLY writer to project_state.json via get_project_context() + Haiku synthesis; CLAUDE.md, CODE.md, PROJECT.md all regenerated from single JSON state; Work item hierarchy: unified mem_work_items with wi_type (use_case/feature/bug/task/requirement), user_status TEXT (open/pending/in-progress/review/done), wi_parent_id linking children to use_case parents; wi_id progression: AI#### (draft) → UC/FE/BU/TA#### (approved)
Last work (2026-05-03): I would like to write markeitng data about AgentDesk - can you create a doc (can be md format) under the root project docs. I would like a short prese