__pycache__/
*.pyc
*.pyo
.env
.env.*
node_modules/
.DS_Store
*.log
.venv/
venv/

# ── Secrets — never commit ─────────────────────────────────────
.envrc
*.pem
*.key
*.p12
*.pfx
*.cert
secrets/
credentials/
**/.git_token
**/.env
**/.env.*
**/.envrc
**/*.pem
**/*.key
**/*.p12
**/*.pfx
**/*.cert
**/secrets/
**/credentials/

# ── Backend data (API keys, billing, user accounts) ───────────
ui/backend/data/
old/ui/backend/data/
backend/data/api_keys.json
backend/data/coupons.json
backend/data/provider_balances.json
backend/data/provider_usage/

# ── Old CLI runtime data (migrated to ui/backend/data/) ──────
.aicli/
**/.aicli/

# ── _system: keep history/memory/context; ignore secrets+runtime ──
# Secrets inside _system
workspace/**/_system/.git_token

# Large Claude Code session transcripts (not needed in git)
workspace/**/_system/sessions/

# aicli CLI runtime data (session store, message history)
workspace/**/_system/aicli/sessions/
