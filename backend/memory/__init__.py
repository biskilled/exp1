# memory — 5-layer memory management for aicli
#
# Modules:
#   sessions.py  — JSON-file session store (message lists for LLM context continuity)
#   embeddings.py — Layer 4 semantic upgrade: pgvector chunking + similarity search
